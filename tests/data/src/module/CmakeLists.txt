if(WIN32)
	set(CMAKE_SHARED_LIBRARY_PREFIX "")
endif()

add_library(module SHARED module.cpp)
target_link_libraries(module RaindropEngine)
set_target_properties(module PROPERTIES RUNTIME_OUTPUT_DIRECTORY ${DATA_OUTPUT_DIRECTORY})